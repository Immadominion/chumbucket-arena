/**
 * TEMP public preview of the ChumBucket hub — renders the /arena hub outside the
 * auth gate so the design can be reviewed with live public match data. Delete before ship.
 */
export { default } from "@/app/(app)/arena/page";
