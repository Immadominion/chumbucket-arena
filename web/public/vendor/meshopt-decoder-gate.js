// Loaded by @google/model-viewer's CachingGLTFLoader.setMeshoptDecoderLocation as a
// classic (non-module) <script>. It only gates readiness — the real MeshoptDecoder
// instance is model-viewer's own statically-bundled import of
// three/examples/jsm/libs/meshopt_decoder.module.js, whose WASM compiles as soon as
// that module is evaluated, independent of this file's contents. This script exists
// purely so the fetch succeeds and the loader's internal promise chain can proceed.
