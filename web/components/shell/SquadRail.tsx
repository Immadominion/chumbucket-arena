"use client";

/**
 * ChumBucket keeps the app shell to a single left rail, no social/ladder rail.
 * Kept as a client no-op so AppShell's import and layout stay stable.
 */
export default function SquadRail() {
  return null;
}
